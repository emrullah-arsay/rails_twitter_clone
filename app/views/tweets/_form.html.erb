<%= form_for(@tweet) do |f| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    
    <div class="form-group">
        <%= f.label :image, class: 'col-sm-2 control-label'  %>
        <div class="col-sm-6">
            <%= f.file_field :image %>
        </div>
    </div>

    <div class="form-group">
        
        <div class="col-sm-12 col-sm-offset-" style="margin:10px">
            <%= f.text_area :body, autofocus: true, class: 'form-control'  %>
        </div>
    </div>
    </br></br>
   <div class="form-group actions">
   
        <div class="col-sm-offset-8 col-sm-6" style="float:left,margin-left:20px">
            <%= f.submit "Create", class: "btn btn-default" ,style:'margin-left:40px' %>
        </div>
 
  </div>

<% end %>
